// Generated by CoffeeScript 1.9.3
(function() {
  var Www, core,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  core = require('./core');

  Www = (function() {
    function Www() {
      this._onDisconnect = bind(this._onDisconnect, this);
      this._onConnection = bind(this._onConnection, this);
    }

    Www.prototype.main = function(app, server) {
      core.app = app;
      core.server = server;
      core.io = require('socket.io').listen(core.server);
      core.www = this;
      core.app.locals.core = core;
      return core.io.sockets.on('connection', this._onConnection);
    };


    /**
     * @protected
     * @param socket
     */

    Www.prototype._onConnection = function(socket) {
      console.log('a user connected');
      return socket.on('disconnect', this._onDisconnect);
    };


    /**
     * @protected
     */

    Www.prototype._onDisconnect = function() {
      return console.log('user disconnected');
    };

    return Www;

  })();

  module.exports = new Www();

}).call(this);

//# sourceMappingURL=www.js.map
