// Generated by CoffeeScript 1.9.3
(function() {
  var ProgressFactory,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  ProgressFactory = (function() {
    ProgressFactory.prototype.modalInstance = null;

    ProgressFactory.prototype.value = 0;

    ProgressFactory.prototype.message = '';

    function ProgressFactory($modal) {
      this.$modal = $modal;
      this.set = bind(this.set, this);
      this.close = bind(this.close, this);
      this.open = bind(this.open, this);
    }

    ProgressFactory.prototype.open = function() {
      this.message = 'processing...';
      this.value = 0;
      return this.modalInstance = this.$modal.open({
        templateUrl: 'progress-modal',
        controller: 'ProgressModalController',
        backdrop: 'static',
        keyboard: false,
        size: 'sm',
        animation: false
      });
    };

    ProgressFactory.prototype.close = function() {
      return this.modalInstance.close();
    };

    ProgressFactory.prototype.set = function(message, value) {
      if (value == null) {
        value = null;
      }
      this.message = message;
      if (value !== null) {
        return this.value = value;
      }
    };

    return ProgressFactory;

  })();

  app.service('progress', ['$modal', ProgressFactory]);

  module.exports = ProgressFactory;

}).call(this);

//# sourceMappingURL=progress.js.map
